# Public keys

Generate the key pairs as described in the deployment README and keep only
these public files in this directory:

- `cache-public-key`
- `admin-ssh.pub`
- `veyon-public-key.pem`

Never commit the corresponding private keys.
